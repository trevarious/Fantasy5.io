
// Extend the Window interface within the component file
interface Window {
    ethereum?: any; // You can replace 'any' with a more specific type if needed
    web3?: any; // You can replace 'any' with a more specific type if needed
}

// Define the shape of UserAccountState if needed
interface UserAccountState {
    // For example: address: string;
}